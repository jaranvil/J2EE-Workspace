package edu.novascotia.training.utitlty;

/**
 *
 * @author W0296430 holds Application Configuration parameter constants.
 *         Normally, an interface should be used to define methods' signature,
 *         and not only constants.
 */
public interface Configuration
{
    /** Where configuration file exists. */
    String CONFIGURATION_FILE = "/project/config.txt";

    /**literal for Assignment. */
    String ASSIGNMENT_LITERAL = "assignment";

    /**flag for enabling debug mode. */
    boolean DEBUG_MODE = true;

}
